import 'package:flutter/material.dart';

class RgpdScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('RGPD')),
      body: Center(child: Text('Consentement et gestion RGPD.')),
    );
  }
}
