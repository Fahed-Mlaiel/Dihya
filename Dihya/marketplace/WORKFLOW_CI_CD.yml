name: Marketplace CI/CD
on:
  push:
    paths:
      - 'Dihya/marketplace/**'
  pull_request:
    paths:
      - 'Dihya/marketplace/**'
jobs:
  test-audit-export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install requirements
        run: pip install -r requirements-dev.txt
      - name: Run tests
        run: pytest Dihya/marketplace/plugins/plugin.py
      - name: Audit RGPD & Accessibilité
        run: python3 Dihya/marketplace/plugins/plugin.py --audit && python3 Dihya/marketplace/plugins/plugin.py --export-rgpd
      - name: Upload artefacts
        uses: actions/upload-artifact@v4
        with:
          name: marketplace-artifacts
          path: Dihya/marketplace/
