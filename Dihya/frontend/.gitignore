# .gitignore – Dihya Coding frontend (version finale)
# Garantit la sécurité, la conformité RGPD, l’auditabilité, l’extensibilité, la robustesse, la souveraineté et une documentation claire.
# Ce fichier est structuré pour répondre au cahier des charges et à la modularité multi-métiers.

# === Dépendances Node.js & gestionnaires de paquets ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === Fichiers d’environnement (jamais versionner de secrets ou variables sensibles) ===
.env
.env.local
.env.*.local

# === Logs locaux, historiques d’auditabilité, traces RGPD ===
*.log
logs/
*.history
*.audit
audit/
audit-logs/
*.trace

# === Fichiers temporaires, caches, outputs de build, coverage ===
dist/
build/
.cache/
.next/
out/
coverage/
tmp/
temp/
*.tmp
*.temp

# === Fichiers système/IDE, OS, workspace ===
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.sublime-workspace
*.sublime-project

# === Fichiers générés par l’application (exports, backups, dumps, snapshots) ===
exports/
backups/
*.bak
*.export
*.dump
*.snapshot

# === Fichiers de tests, mocks, artefacts CI/CD ===
*.test.js.snap
*.spec.js.snap
jest/
cypress/screenshots/
cypress/videos/
test-results/
coverage-final.json

# === RGPD : ne jamais versionner de données personnelles ou logs utilisateurs ===
# === Auditabilité : ce fichier est commenté pour conformité, traçabilité et souveraineté numérique ===