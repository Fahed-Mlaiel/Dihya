# .envrc – Configuration direnv pour Dihya Coding
#
# Ce fichier permet de charger automatiquement les variables d’environnement locales pour le développement.
#
# Sécurité : Ne jamais versionner ce fichier avec des secrets réels.
# RGPD : Aucune donnée personnelle ne doit être stockée ici.
# Accessibilité : Documenter chaque variable pour faciliter la maintenance.

# Exemple :
# export DATABASE_URL=postgresql://user:password@localhost:5432/dihya
# export SECRET_KEY=dev-secret
# ...

# Pour la liste complète, voir la documentation interne.

export APP_ENV=development
export APP_DEBUG=true
export SECRET_KEY="dev_secret_key"
export DATABASE_URL="sqlite:///dev.db"
export JWT_SECRET="dev_jwt_secret"
export CORS_ORIGINS="*"
export LOG_LEVEL=debug
export RBAC_ENABLED=true
export MULTITENANCY=false
export INTERNATIONALIZATION="fr,en,ar,de,es,it,pt,ru,zh,ja,ko,tr,pl"

# Variables d’environnement pour le développement local
export SECRET_KEY=dev_secret
export DATABASE_URL=sqlite:///dihya_dev.db
export JWT_SECRET=dev_jwt_secret
export MAIL_SERVER=localhost
export MAIL_PORT=1025
export MAIL_USERNAME=dev@example.com
export MAIL_PASSWORD=dev
export CORS_ORIGINS=http://localhost:3000
export BASE_URL=http://localhost:5000
export ENV=development
export DEBUG=True

# Variables d’environnement pour la production
# export $(grep -v '^#' .env.production | xargs)

# Pour la documentation complète, voir docs/API_REFERENCE.md

# direnv Konfigurationsdatei
# Beispiel: export ENV=production
# Siehe https://direnv.net/
