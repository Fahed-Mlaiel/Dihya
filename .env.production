# .env.production – Variables d’environnement (production)
#
# Ce fichier contient les variables d’environnement nécessaires au déploiement sécurisé de Dihya Coding en production.
#
# Sécurité : Ne jamais versionner ce fichier avec des secrets réels. Utiliser un gestionnaire de secrets (Vault, AWS Secrets Manager, etc.).
# RGPD : Aucune donnée personnelle ne doit être stockée ici.
# Accessibilité : Documenter chaque variable pour faciliter la maintenance.
# CI/CD : Ce fichier est utilisé par les pipelines de déploiement.

# Exemple :
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# SECRET_KEY=change-me
# JWT_SECRET=change-me
# SENTRY_DSN=
# ...

# Pour la liste complète, voir la documentation interne.

# --- Python/Flask ---
FLASK_ENV=production
FLASK_APP=Dihya/backend/flask/app/main.py
PYTHONPATH=$PWD/Dihya/backend/flask/app

# --- Node.js/React ---
NODE_ENV=production

# --- IA & Souveraineté ---
# Clés à injecter via GitHub Actions secrets ou vault sécurisé, jamais en clair ici
OPENAI_API_KEY=${OPENAI_API_KEY}
OPENAI_MODEL=gpt-4o
MIXTRAL_ENABLED=1
LLAMA_ENABLED=1
MISTRAL_ENABLED=1

# --- Multilingue ---
DIHYA_LANGUAGES=fr,en,ar,tzr

# --- Sécurité & Souveraineté ---
DIHYA_SOUVERAINETE=1
DIHYA_DEBUG=0
DIHYA_SECRET_KEY=${DIHYA_SECRET_KEY} # À injecter via secrets CI/CD uniquement

# --- DevOps & CI/CD ---
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# --- Plugins & Marketplace ---
DIHYA_PLUGINS_PATH=$PWD/Dihya/plugins

# --- Accessibilité & RGPD ---
DIHYA_ACCESSIBILITY=1
DIHYA_RGPD=1

# --- PATH ---
PATH=$PWD/Dihya/scripts:$PATH

# --- Production only ---
# Pas de test, pas de mock, pas de debug, pas de logs verbeux
# Toute variable sensible doit être injectée par le pipeline CI/CD (GitHub Actions, Vault, etc.)

# Sécurité : ce fichier ne doit jamais contenir de secrets en clair, ni être exposé publiquement.
# RGPD : aucune donnée personnelle, logs minimisés, auditabilité garantie.
# Multilingue : voir docs/devops pour les guides en fr, en, ar, amazigh.
# Accessibilité : ce fichier est commenté et documenté pour tous les profils.

# Fichier d’environnement de production Dihya Coding
# Sécurité maximale, RGPD, CI/CD-ready, multilingue, auditabilité
# Ne jamais versionner ce fichier en clair !
# Exemple de variables (à adapter et chiffrer via Vault/SOPS)
APP_ENV=production
APP_DEBUG=false
SECRET_KEY="${PROD_SECRET_KEY}"
DATABASE_URL="${PROD_DATABASE_URL}"
JWT_SECRET="${PROD_JWT_SECRET}"
CORS_ORIGINS="https://dihya.app,https://*.dihya.app"
LOG_LEVEL=info
MONITORING_ENABLED=true
BACKUP_ENABLED=true
RBAC_ENABLED=true
MULTITENANCY=true
INTERNATIONALIZATION=fr,en,ar,de,es,it,pt,ru,zh,ja,ko,tr,pl

# Pour plus de détails, voir SECURITY.md et la documentation CI/CD.

# Produktionsumgebungsvariablen
# Beispiel: API_KEY=dein_api_key
# Bitte sichere diese Datei und teile sie nicht öffentlich!
